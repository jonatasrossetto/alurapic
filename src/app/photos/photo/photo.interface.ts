//interface necessário para tipar o retorno do backend permitindo importar corretamente as chaves do json recebido da web api do backend
export interface PhotoInterface {
    url:string;
    description:string;
}